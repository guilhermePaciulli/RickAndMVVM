# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TestingMVVM' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TestingMVVM
  pod 'Kingfisher', '~> 5.0'
  pod 'RxSwift',    '~> 4.0'
  pod 'RxCocoa',    '~> 4.0'
  pod "RxSwiftExt"

  target 'TestingMVVMTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TestingMVVMUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
